meta {
  name: Delete
  type: http
  seq: 4
}

delete {
  url: {{host}}/{{endpoint}}/{{mediaType}}/{{subPath}}/{{mediaId}}
  body: none
  auth: inherit
}

vars:pre-request {
  mediaType: movie
  mediaId: 4638
}
