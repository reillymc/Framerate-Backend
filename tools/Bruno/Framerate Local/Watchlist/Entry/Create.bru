meta {
  name: Create
  type: http
  seq: 5
}

post {
  url: {{host}}/{{endpoint}}/{{mediaType}}/{{subPath}}
  body: json
  auth: inherit
}

body:json {
  {
    "mediaId": 4638
  }
}

vars:pre-request {
  mediaType: movie
}
