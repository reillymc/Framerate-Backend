meta {
  name: Create
  type: http
  seq: 4
}

post {
  url: {{host}}/{{endpoint}}/{{subPath}}/{{watchlistId}}
  body: json
  auth: inherit
}

body:json {
  {
    "showId": 194764
  }
}

vars:pre-request {
  watchlistId: default
}
