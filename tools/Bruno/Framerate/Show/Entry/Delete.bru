meta {
  name: Delete
  type: http
  seq: 3
}

delete {
  url: {{host}}/{{endpoint}}/{{subPath}}/{{watchlistId}}/{{showId}}
  body: none
  auth: inherit
}

vars:pre-request {
  showId: 194764
  watchlistId: default
}
