meta {
  name: Delete
  type: http
  seq: 4
}

delete {
  url: {{host}}/{{endpoint}}/{{subpath}}/{{watchlistId}}/{{movieId}}
  body: none
  auth: inherit
}

vars:pre-request {
  movieId: 4638
  watchlistId: default
}
